#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Testing build before push..."
npm run build || {
  echo "❌ Build failed! Fix errors before pushing."
  echo "This prevents failed Vercel deployments."
  exit 1
}